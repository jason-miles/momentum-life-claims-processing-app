# AI layer runtime dependencies (Synopsis Agent + index build + eval).
# The Streamlit app imports ai.agents.synopsis_agent, which needs these.
databricks-sdk>=0.30.0
databricks-vectorsearch>=0.40
openai>=1.55.0
typing_extensions>=4.12.0
mlflow>=2.15.0
